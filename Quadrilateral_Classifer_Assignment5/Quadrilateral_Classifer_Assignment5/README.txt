When you run "make fuzzer" my makefile will compile all necessary parts for the program to run. My shell script "fuzz.sh" which is essentially my driver for everything to be triggered can be run with "./fuzz.sh" AFTER you run "make fuzzer", as per the assignment instructions.